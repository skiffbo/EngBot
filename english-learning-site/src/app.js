const lessons = [
  {
    id: "bbc-ebola",
    sourceKey: "bbc",
    sourceLabel: "BBC 新闻学习改写",
    title: "Health Teams Race to Stop an Ebola Outbreak",
    route: "周一、周三、周五：BBC 新闻主题",
    sourceUrl: "https://www.bbc.com/news/articles/cz72p75zg4qo",
    sourceName: "BBC News",
    paragraph:
      "Health workers in a remote part of the Democratic Republic of Congo are trying to slow an Ebola outbreak before it reaches larger towns. The disease can spread through close contact with the body fluids of an infected person, so nurses must wear protective clothing and families must change the way they care for the sick. Local roads are poor, mobile phone signals are weak, and many villages are far from hospitals. These problems make it harder to find patients quickly, trace the people they met, and explain why isolation matters. Even so, doctors say clear information and early treatment can save lives, especially when communities trust the health teams who arrive to help.",
    translation:
      "剛果民主共和國偏遠地區的醫護人員，正努力在埃博拉疫情擴散到較大的城鎮之前減緩它的傳播。這種疾病可能透過與感染者體液的近距離接觸而傳播，因此護士必須穿著防護衣，家屬也必須改變照顧病人的方式。當地道路狀況不佳，手機訊號薄弱，許多村莊離醫院很遠。這些問題使人們更難快速找到病人、追蹤他們接觸過的人，並解釋為什麼隔離很重要。即使如此，醫生表示，清楚的資訊和早期治療可以挽救生命，尤其是在社區信任前來協助的醫療團隊時。",
    sentenceTranslations: [
      "剛果民主共和國偏遠地區的醫護人員，正努力在埃博拉疫情擴散到較大的城鎮之前減緩它的傳播。",
      "這種疾病可能透過與感染者體液的近距離接觸而傳播，因此護士必須穿著防護衣，家屬也必須改變照顧病人的方式。",
      "當地道路狀況不佳，手機訊號薄弱，許多村莊離醫院很遠。",
      "這些問題使人們更難快速找到病人、追蹤他們接觸過的人，並解釋為什麼隔離很重要。",
      "即使如此，醫生表示，清楚的資訊和早期治療可以挽救生命，尤其是在社區信任前來協助的醫療團隊時。"
    ],
    grammar: [
      {
        sentence:
          "Health workers in a remote part of the Democratic Republic of Congo are trying to slow an Ebola outbreak before it reaches larger towns.",
        formula: "S + Vt(进行时) + O + Adv-Clause",
        explanation:
          "Health workers = S；are trying = Vt 短语，现在进行时；to slow an Ebola outbreak = O/目的不定式；before it reaches larger towns = 时间状语从句。主句表示正在进行的行动，before 从句用一般现在时表示将来的时间条件。"
      },
      {
        sentence:
          "The disease can spread through close contact with the body fluids of an infected person, so nurses must wear protective clothing and families must change the way they care for the sick.",
        formula: "S + Modal + Vi + Adv + Conj + S + Modal + Vt + O",
        explanation:
          "The disease = S；can spread = Modal + Vi；through close contact... = 方式状语；so 连接结果；nurses/families = S；must wear/must change = Modal + Vt；protective clothing/the way = O。can 与 must 后面都接动词原形。"
      },
      {
        sentence:
          "Local roads are poor, mobile phone signals are weak, and many villages are far from hospitals.",
        formula: "S + be + C, S + be + C, and S + be + PrepP",
        explanation:
          "Local roads/mobile phone signals/many villages = 三个并列 S；are = linking V；poor/weak/far from hospitals = C。一般现在时描述客观状态。"
      },
      {
        sentence:
          "These problems make it harder to find patients quickly, trace the people they met, and explain why isolation matters.",
        formula: "S + Vt + O + C + Infinitive List",
        explanation:
          "These problems = S；make = Vt；it = 形式 O；harder = O 的补足语 C；to find/trace/explain = 真正要表达的动作。why isolation matters 是宾语从句，isolation = S，matters = Vi。"
      },
      {
        sentence:
          "Even so, doctors say clear information and early treatment can save lives, especially when communities trust the health teams who arrive to help.",
        formula: "Adv + S + Vt + O-Clause + Adv-Clause",
        explanation:
          "doctors = S；say = Vt；clear information and early treatment can save lives 是宾语从句，其中并列名词短语作 S，can save = Modal + Vt，lives = O；when communities trust... = 时间/条件状语从句；who arrive to help 修饰 health teams。"
      }
    ]
  },
  {
    id: "bbc-storm",
    sourceKey: "bbc",
    sourceLabel: "BBC 新闻学习改写",
    title: "A Coastal Town Responds After a Storm",
    route: "周一、周三、周五：BBC 新闻主题",
    sourceUrl: "https://www.bbc.com/news/topics/cvenzmgyww4t",
    sourceName: "BBC Weather and climate coverage",
    paragraph:
      "When a powerful storm damaged roads in a coastal town, many families had to leave their homes for several days. Reporters said the heaviest rain fell overnight, when most people were sleeping and emergency teams were already preparing for floods. By morning, teachers, shop owners and volunteers were carrying food, blankets and clean water to a sports centre that had become a temporary shelter. Some teenagers helped by translating messages for older neighbours who did not use smartphones. Officials warned that rebuilding would take time because bridges and power lines needed careful checks. The story shows how extreme weather can interrupt ordinary life, but it also shows how calm planning and shared responsibility can protect a community.",
    translation:
      "當一場強烈風暴破壞沿海城鎮的道路時，許多家庭不得不離家數日。記者表示，最大雨勢出現在夜間，當時多數人正在睡覺，救援隊伍也已經在為洪水做準備。到了早晨，教師、店主和志工把食物、毯子與乾淨的水送到一座已成為臨時避難所的體育中心。有些青少年透過幫不用智慧型手機的年長鄰居翻譯訊息來提供協助。官員警告，重建需要時間，因為橋梁和電力線必須仔細檢查。這則故事顯示極端天氣會打斷日常生活，但也顯示冷靜的計畫與共同責任可以保護一個社區。",
    sentenceTranslations: [
      "當一場強烈風暴破壞沿海城鎮的道路時，許多家庭不得不離家數日。",
      "記者表示，最大雨勢出現在夜間，當時多數人正在睡覺，救援隊伍也已經在為洪水做準備。",
      "到了早晨，教師、店主和志工把食物、毯子與乾淨的水送到一座已成為臨時避難所的體育中心。",
      "有些青少年透過幫不用智慧型手機的年長鄰居翻譯訊息來提供協助。",
      "官員警告，重建需要時間，因為橋梁和電力線必須仔細檢查。",
      "這則故事顯示極端天氣會打斷日常生活，但也顯示冷靜的計畫與共同責任可以保護一個社區。"
    ],
    grammar: [
      {
        sentence:
          "When a powerful storm damaged roads in a coastal town, many families had to leave their homes for several days.",
        formula: "Adv-Clause + S + Modal-like V + O + Adv",
        explanation:
          "When 引导时间状语从句；a powerful storm = S，damaged = Vt，roads = O；主句 many families = S，had to leave = 必须做某事，their homes = O，for several days = 时间状语。全句用一般过去时叙述已发生事件。"
      },
      {
        sentence:
          "Reporters said the heaviest rain fell overnight, when most people were sleeping and emergency teams were already preparing for floods.",
        formula: "S + Vt + O-Clause + Adv-Clause",
        explanation:
          "Reporters = S；said = Vt；the heaviest rain fell overnight 是 O-Clause。when 从句说明同时发生的背景动作，were sleeping/were preparing 是过去进行时。"
      },
      {
        sentence:
          "By morning, teachers, shop owners and volunteers were carrying food, blankets and clean water to a sports centre that had become a temporary shelter.",
        formula: "Adv + S + Vt(进行时) + O + PrepP + Relative-Clause",
        explanation:
          "By morning = 时间状语；teachers, shop owners and volunteers = 并列 S；were carrying = 过去进行时 Vt；food, blankets and clean water = O；that had become... 是定语从句，用过去完成时表示更早已经变成避难所。"
      },
      {
        sentence:
          "Some teenagers helped by translating messages for older neighbours who did not use smartphones.",
        formula: "S + Vi + Gerund Phrase + Relative-Clause",
        explanation:
          "Some teenagers = S；helped = Vi；by translating messages = 方式状语，by 后接动名词；who did not use smartphones 修饰 older neighbours。"
      },
      {
        sentence:
          "Officials warned that rebuilding would take time because bridges and power lines needed careful checks.",
        formula: "S + Vt + O-Clause + Adv-Clause",
        explanation:
          "Officials = S；warned = Vt；that rebuilding would take time = O-Clause，rebuilding 是动名词作 S，would take 表示过去语境中的将来；because 从句说明原因。"
      },
      {
        sentence:
          "The story shows how extreme weather can interrupt ordinary life, but it also shows how calm planning and shared responsibility can protect a community.",
        formula: "S + Vt + O-Clause + Conj + S + Vt + O-Clause",
        explanation:
          "The story/it = S；shows = Vt；how 引导两个宾语从句。extreme weather 与 calm planning and shared responsibility 分别作从句 S；can interrupt/can protect = Modal + Vt。一般现在时用于表达文章给出的普遍启示。"
      }
    ]
  },
  {
    id: "shanghai-library",
    sourceKey: "zhongkao",
    sourceLabel: "上海中考阅读风格改写",
    title: "A Library Corner for Teenagers",
    route: "周二、周四、周六：上海中考英语阅读题主题",
    sourceUrl: "https://www.shmeea.edu.cn/",
    sourceName: "Shanghai education exam materials",
    paragraph:
      "Every morning, Li Ming passes a small community library on his way to school. At first, he thought the room was only for younger children, because the shelves were full of picture books and quiet games. One rainy day, however, he went inside and found a corner for teenagers with science magazines, short novels and a notice board. The librarian invited him to join a weekend reading group where students discuss one article and share new words. After several weeks, Li Ming became more confident in English because he no longer read every sentence word by word. He learned to guess meanings from context, mark useful phrases and ask better questions.",
    translation:
      "每天早晨，李明上學途中都會經過一間小型社區圖書館。起初，他以為那個房間只給年紀較小的孩子使用，因為書架上擺滿了圖畫書和安靜的遊戲。然而，在一個下雨天，他走進去，發現有一個給青少年的角落，裡面有科學雜誌、短篇小說和公告欄。圖書管理員邀請他加入週末閱讀小組，在那裡學生會討論一篇文章並分享新單詞。幾週後，李明在英語方面變得更有信心，因為他不再逐字逐句地閱讀。他學會了根據上下文猜測意思、標記有用片語，並提出更好的問題。",
    sentenceTranslations: [
      "每天早晨，李明上學途中都會經過一間小型社區圖書館。",
      "起初，他以為那個房間只給年紀較小的孩子使用，因為書架上擺滿了圖畫書和安靜的遊戲。",
      "然而，在一個下雨天，他走進去，發現有一個給青少年的角落，裡面有科學雜誌、短篇小說和公告欄。",
      "圖書管理員邀請他加入週末閱讀小組，在那裡學生會討論一篇文章並分享新單詞。",
      "幾週後，李明在英語方面變得更有信心，因為他不再逐字逐句地閱讀。",
      "他學會了根據上下文猜測意思、標記有用片語，並提出更好的問題。"
    ],
    grammar: [
      {
        sentence:
          "Every morning, Li Ming passes a small community library on his way to school.",
        formula: "Adv + S + Vt + O + Adv",
        explanation:
          "Every morning = 时间状语；Li Ming = S；passes = Vt；a small community library = O；on his way to school = 地点/情境状语。一般现在时表示每天重复发生的习惯。"
      },
      {
        sentence:
          "At first, he thought the room was only for younger children, because the shelves were full of picture books and quiet games.",
        formula: "Adv + S + Vt + O-Clause + Adv-Clause",
        explanation:
          "he = S；thought = Vt；the room was only for younger children 是 O-Clause，其中 was 是 linking V；because 从句说明原因，the shelves = S，were full of = be + Adj/Prep 结构。"
      },
      {
        sentence:
          "One rainy day, however, he went inside and found a corner for teenagers with science magazines, short novels and a notice board.",
        formula: "Adv + S + Vi + Adv + Conj + Vt + O",
        explanation:
          "One rainy day = 时间状语；however = 转折副词；he = S；went = Vi，inside = Adv；found = Vt；a corner = O；with... 说明角落里有什么。一般过去时叙述具体一天发生的事。"
      },
      {
        sentence:
          "The librarian invited him to join a weekend reading group where students discuss one article and share new words.",
        formula: "S + Vt + O + OC(to-inf) + Relative-Clause",
        explanation:
          "The librarian = S；invited = Vt；him = O；to join... = O 的补足语，说明被邀请去做什么；where 引导定语从句修饰 group。discuss/share 用一般现在时，表示小组的常规活动。"
      },
      {
        sentence:
          "After several weeks, Li Ming became more confident in English because he no longer read every sentence word by word.",
        formula: "Adv + S + Vl + C + Adv-Clause",
        explanation:
          "After several weeks = 时间状语；Li Ming = S；became = linking V；more confident = C；because 从句说明原因。no longer 表示“不再”，read 用一般过去时。"
      },
      {
        sentence:
          "He learned to guess meanings from context, mark useful phrases and ask better questions.",
        formula: "S + Vt + O(to-inf list)",
        explanation:
          "He = S；learned = Vt；to guess, mark and ask 是三个并列不定式动作，合起来作 O。learned 用一般过去时，表示已经学会的能力。"
      }
    ]
  },
  {
    id: "shanghai-garden",
    sourceKey: "zhongkao",
    sourceLabel: "上海中考阅读风格改写",
    title: "A School Garden Project",
    route: "周二、周四、周六：上海中考英语阅读题主题",
    sourceUrl: "https://www.shmeea.edu.cn/",
    sourceName: "Shanghai education exam materials",
    paragraph:
      "In 2024, a middle school in Shanghai opened a small garden beside its playground. Students had to plan the garden by themselves, so they measured the land, chose vegetables and wrote a weekly care schedule. Some students were surprised that tomatoes needed more sunlight than they expected, while others found that watering too often could hurt the roots. During English class, the teacher asked the class to keep a garden diary in simple sentences. The project helped students connect new words with real actions. It also taught them that making mistakes, checking facts and trying again are part of learning, whether they are growing plants or studying a language.",
    translation:
      "2024年，上海一所中學在操場旁開闢了一座小花園。學生必須自己規劃花園，因此他們測量土地、選擇蔬菜，並寫下每週照顧計畫。有些學生很驚訝地發現，番茄需要的陽光比他們原先預期的更多；另一些學生則發現，澆水太頻繁可能會傷害根部。在英語課上，老師要求全班用簡單句寫花園日記。這個專案幫助學生把新單詞與真實行動連結起來。它也教會他們：犯錯、查證事實並再次嘗試，都是學習的一部分，不論他們是在種植物，還是在學一門語言。",
    sentenceTranslations: [
      "2024年，上海一所中學在操場旁開闢了一座小花園。",
      "學生必須自己規劃花園，因此他們測量土地、選擇蔬菜，並寫下每週照顧計畫。",
      "有些學生很驚訝地發現，番茄需要的陽光比他們原先預期的更多；另一些學生則發現，澆水太頻繁可能會傷害根部。",
      "在英語課上，老師要求全班用簡單句寫花園日記。",
      "這個專案幫助學生把新單詞與真實行動連結起來。",
      "它也教會他們：犯錯、查證事實並再次嘗試，都是學習的一部分，不論他們是在種植物，還是在學一門語言。"
    ],
    grammar: [
      {
        sentence:
          "In 2024, a middle school in Shanghai opened a small garden beside its playground.",
        formula: "Adv + S + Vt + O + PrepP",
        explanation:
          "In 2024 = 时间状语；a middle school in Shanghai = S；opened = Vt；a small garden = O；beside its playground = 地点状语。一般过去时说明 2024 年发生的事。"
      },
      {
        sentence:
          "Students had to plan the garden by themselves, so they measured the land, chose vegetables and wrote a weekly care schedule.",
        formula: "S + Modal-like V + O + Conj + S + Vt + O List",
        explanation:
          "Students = S；had to plan = 必须做某事；the garden = O；so 连接结果。they = S；measured/chose/wrote = 三个并列 Vt；land/vegetables/schedule = 对应 O。"
      },
      {
        sentence:
          "Some students were surprised that tomatoes needed more sunlight than they expected, while others found that watering too often could hurt the roots.",
        formula: "S + be + C + O-Clause + Adv-Clause",
        explanation:
          "Some students = S；were surprised = be + Adj 作 C；that 从句解释惊讶的内容。while 引导对比从句；watering too often 是动名词短语作 S；could hurt = Modal + Vt。"
      },
      {
        sentence:
          "During English class, the teacher asked the class to keep a garden diary in simple sentences.",
        formula: "Adv + S + Vt + O + OC(to-inf)",
        explanation:
          "During English class = 时间状语；the teacher = S；asked = Vt；the class = O；to keep a garden diary = O 的补足语；in simple sentences = 方式状语。"
      },
      {
        sentence:
          "The project helped students connect new words with real actions.",
        formula: "S + Vt + O + OC(base V)",
        explanation:
          "The project = S；helped = Vt；students = O；connect new words with real actions = O 的补足语。help sb. do sth. 中 do 使用动词原形。"
      },
      {
        sentence:
          "It also taught them that making mistakes, checking facts and trying again are part of learning, whether they are growing plants or studying a language.",
        formula: "S + Vt + O + O-Clause + Adv-Clause",
        explanation:
          "It = S；taught = Vt；them = O；that 引导宾语从句。making/checking/trying 是三个动名词短语作并列 S，所以用 are。whether...or... 表示两种情况。"
      }
    ]
  }
];

const vocab = {
  remote: {
    ipa: "/rɪˈməʊt/",
    pos: "adj.",
    meaning: "遙遠的；偏僻的。常用來形容地點離城市或中心很遠。",
    examples: [
      "The clinic is in a remote village near the mountains.",
      "Students used a map to find the remote island."
    ]
  },
  ebola: {
    ipa: "/iˈboʊlə/",
    pos: "n.",
    meaning: "埃博拉，一種嚴重傳染病的名稱。",
    examples: [
      "Doctors worked carefully during the Ebola outbreak.",
      "People learned how Ebola can spread from person to person."
    ]
  },
  outbreak: {
    ipa: "/ˈaʊtbreɪk/",
    pos: "n.",
    meaning: "爆發；突然發生，常指疾病、戰爭或問題的突然出現。",
    examples: [
      "The town prepared quickly after the disease outbreak.",
      "Clear information can reduce fear during an outbreak."
    ]
  },
  reaches: {
    ipa: "/ˈriːtʃɪz/",
    pos: "v.",
    meaning: "到達；抵達。原形是 reach。",
    examples: [
      "The train reaches the station at eight.",
      "News reaches small villages slowly when signals are weak."
    ]
  },
  spread: {
    ipa: "/spred/",
    pos: "v./n.",
    meaning: "傳播；擴散；也可作名詞表示擴散。",
    examples: [
      "The disease can spread through close contact.",
      "Good habits help stop the spread of illness."
    ]
  },
  contact: {
    ipa: "/ˈkɑːntækt/",
    pos: "n./v.",
    meaning: "接觸；聯絡。作名詞時重音常在前。",
    examples: [
      "Close contact can make some diseases spread faster.",
      "Please contact the teacher if you have a question."
    ]
  },
  fluids: {
    ipa: "/ˈfluːɪdz/",
    pos: "n.",
    meaning: "液體；體液。單數是 fluid。",
    examples: [
      "Nurses must be careful with body fluids.",
      "Water and oil are different kinds of fluids."
    ]
  },
  infected: {
    ipa: "/ɪnˈfektɪd/",
    pos: "adj.",
    meaning: "受感染的；被感染的。",
    examples: [
      "An infected person should receive medical help quickly.",
      "Doctors wore masks when they visited infected patients."
    ]
  },
  protective: {
    ipa: "/prəˈtektɪv/",
    pos: "adj.",
    meaning: "保護性的；防護的。",
    examples: [
      "The nurse wore protective clothing in the hospital.",
      "A helmet is protective when you ride a bike."
    ]
  },
  signals: {
    ipa: "/ˈsɪɡnəlz/",
    pos: "n.",
    meaning: "訊號；信號。單數是 signal。",
    examples: [
      "Mobile phone signals are weak in the mountains.",
      "The traffic signals changed from red to green."
    ]
  },
  patients: {
    ipa: "/ˈpeɪʃnts/",
    pos: "n.",
    meaning: "病人；患者。單數是 patient。",
    examples: [
      "The doctor visited three patients in the morning.",
      "Patients need clear information and kind care."
    ]
  },
  trace: {
    ipa: "/treɪs/",
    pos: "v./n.",
    meaning: "追蹤；查出；痕跡。",
    examples: [
      "Health workers trace the people a patient has met.",
      "The police found a trace of paint on the road."
    ]
  },
  isolation: {
    ipa: "/ˌaɪsəˈleɪʃn/",
    pos: "n.",
    meaning: "隔離；孤立。",
    examples: [
      "Isolation can stop a disease from spreading.",
      "The old village lived in isolation for many years."
    ]
  },
  treatment: {
    ipa: "/ˈtriːtmənt/",
    pos: "n.",
    meaning: "治療；對待方式。",
    examples: [
      "Early treatment can save lives.",
      "The patient received careful treatment at the clinic."
    ]
  },
  communities: {
    ipa: "/kəˈmjuːnətiz/",
    pos: "n.",
    meaning: "社區；群體。單數是 community。",
    examples: [
      "Strong communities can solve problems together.",
      "Different communities shared food after the storm."
    ]
  },
  trust: {
    ipa: "/trʌst/",
    pos: "v./n.",
    meaning: "信任；相信。",
    examples: [
      "People trust the doctors because they explain clearly.",
      "Trust is important between friends."
    ]
  },
  powerful: {
    ipa: "/ˈpaʊərfl/",
    pos: "adj.",
    meaning: "強大的；有力的。",
    examples: [
      "A powerful storm damaged roads near the coast.",
      "Her speech was short but powerful."
    ]
  },
  coastal: {
    ipa: "/ˈkoʊstl/",
    pos: "adj.",
    meaning: "沿海的；靠近海岸的。",
    examples: [
      "The family lives in a coastal town.",
      "Coastal cities must prepare for heavy rain."
    ]
  },
  damaged: {
    ipa: "/ˈdæmɪdʒd/",
    pos: "v./adj.",
    meaning: "破壞；損壞的。原形是 damage。",
    examples: [
      "The storm damaged the old bridge.",
      "The damaged road was closed for two days."
    ]
  },
  overnight: {
    ipa: "/ˌoʊvərˈnaɪt/",
    pos: "adv.",
    meaning: "在夜間；一夜之間。",
    examples: [
      "The heaviest rain fell overnight.",
      "The weather changed overnight."
    ]
  },
  emergency: {
    ipa: "/ɪˈmɜːrdʒənsi/",
    pos: "n./adj.",
    meaning: "緊急情況；緊急的。",
    examples: [
      "Emergency teams prepared for floods.",
      "Call this number in an emergency."
    ]
  },
  temporary: {
    ipa: "/ˈtempəreri/",
    pos: "adj.",
    meaning: "臨時的；暫時的。",
    examples: [
      "The sports centre became a temporary shelter.",
      "This is only a temporary solution."
    ]
  },
  shelter: {
    ipa: "/ˈʃeltər/",
    pos: "n./v.",
    meaning: "避難所；庇護；也可作動詞。",
    examples: [
      "Families stayed in a shelter after the flood.",
      "The trees sheltered us from the wind."
    ]
  },
  translating: {
    ipa: "/trænzˈleɪtɪŋ/",
    pos: "v.-ing",
    meaning: "翻譯；原形是 translate。",
    examples: [
      "The students helped by translating messages.",
      "Translating a sentence can help you notice grammar."
    ]
  },
  officials: {
    ipa: "/əˈfɪʃlz/",
    pos: "n.",
    meaning: "官員；工作人員。",
    examples: [
      "Officials warned people to stay away from the river.",
      "School officials checked the safety rules."
    ]
  },
  rebuilding: {
    ipa: "/ˌriːˈbɪldɪŋ/",
    pos: "n./v.-ing",
    meaning: "重建；重新建造。",
    examples: [
      "Rebuilding the bridge will take time.",
      "The team is rebuilding the library after the storm."
    ]
  },
  responsibility: {
    ipa: "/rɪˌspɑːnsəˈbɪləti/",
    pos: "n.",
    meaning: "責任；職責。",
    examples: [
      "Shared responsibility can protect a community.",
      "It is my responsibility to finish the homework."
    ]
  },
  library: {
    ipa: "/ˈlaɪbreri/",
    pos: "n.",
    meaning: "圖書館；藏書室。",
    examples: [
      "Li Ming passes a small library every morning.",
      "I borrowed two novels from the library."
    ]
  },
  community: {
    ipa: "/kəˈmjuːnəti/",
    pos: "n.",
    meaning: "社區；共同生活的人群。",
    examples: [
      "The community library is open on weekends.",
      "A strong community helps every family."
    ]
  },
  shelves: {
    ipa: "/ʃelvz/",
    pos: "n.",
    meaning: "架子；書架。單數是 shelf。",
    examples: [
      "The shelves were full of picture books.",
      "Please put the magazines back on the shelves."
    ]
  },
  teenagers: {
    ipa: "/ˈtiːneɪdʒərz/",
    pos: "n.",
    meaning: "青少年；十幾歲的人。",
    examples: [
      "The library has a corner for teenagers.",
      "Teenagers often enjoy short novels and magazines."
    ]
  },
  librarian: {
    ipa: "/laɪˈbreriən/",
    pos: "n.",
    meaning: "圖書管理員。",
    examples: [
      "The librarian invited him to join the group.",
      "Ask the librarian where the dictionaries are."
    ]
  },
  article: {
    ipa: "/ˈɑːrtɪkl/",
    pos: "n.",
    meaning: "文章；物品；冠詞。",
    examples: [
      "Students discuss one article every weekend.",
      "This article explains how to learn new words."
    ]
  },
  confident: {
    ipa: "/ˈkɑːnfɪdənt/",
    pos: "adj.",
    meaning: "有信心的；自信的。",
    examples: [
      "Li Ming became more confident in English.",
      "Practice makes me confident before a test."
    ]
  },
  context: {
    ipa: "/ˈkɑːntekst/",
    pos: "n.",
    meaning: "上下文；背景。",
    examples: [
      "You can guess meanings from context.",
      "The context helps us understand the new word."
    ]
  },
  phrases: {
    ipa: "/ˈfreɪzɪz/",
    pos: "n.",
    meaning: "片語；短語。單數是 phrase。",
    examples: [
      "Mark useful phrases when you read.",
      "The phrase word by word means one word at a time."
    ]
  },
  several: {
    ipa: "/ˈsevrəl/",
    pos: "det.",
    meaning: "幾個；若干。",
    examples: [
      "After several weeks, he felt more confident.",
      "I read several pages before dinner."
    ]
  },
  notice: {
    ipa: "/ˈnoʊtɪs/",
    pos: "n./v.",
    meaning: "公告；注意到。",
    examples: [
      "There was a notice board in the library.",
      "I noticed a new word in the sentence."
    ]
  },
  discuss: {
    ipa: "/dɪˈskʌs/",
    pos: "v.",
    meaning: "討論。",
    examples: [
      "Students discuss one article in the group.",
      "We discussed the grammar after class."
    ]
  },
  meanings: {
    ipa: "/ˈmiːnɪŋz/",
    pos: "n.",
    meaning: "意思；含義。單數是 meaning。",
    examples: [
      "He learned to guess meanings from context.",
      "Some words have several meanings."
    ]
  },
  garden: {
    ipa: "/ˈɡɑːrdn/",
    pos: "n.",
    meaning: "花園；菜園。",
    examples: [
      "The school opened a small garden.",
      "Students wrote a diary about the garden."
    ]
  },
  measured: {
    ipa: "/ˈmeʒərd/",
    pos: "v.",
    meaning: "測量；原形是 measure。",
    examples: [
      "They measured the land before planting vegetables.",
      "The teacher measured the desk with a ruler."
    ]
  },
  schedule: {
    ipa: "/ˈskedʒuːl/",
    pos: "n./v.",
    meaning: "時間表；計畫表；安排。",
    examples: [
      "Students wrote a weekly care schedule.",
      "My reading schedule helps me study every day."
    ]
  },
  sunlight: {
    ipa: "/ˈsʌnlaɪt/",
    pos: "n.",
    meaning: "陽光。",
    examples: [
      "Tomatoes need more sunlight than some plants.",
      "The room is bright because of the sunlight."
    ]
  },
  roots: {
    ipa: "/ruːts/",
    pos: "n.",
    meaning: "根；根源。單數是 root。",
    examples: [
      "Too much water can hurt the roots.",
      "The tree has strong roots."
    ]
  },
  diary: {
    ipa: "/ˈdaɪəri/",
    pos: "n.",
    meaning: "日記；日誌。",
    examples: [
      "The students kept a garden diary.",
      "I write one English sentence in my diary every night."
    ]
  },
  connect: {
    ipa: "/kəˈnekt/",
    pos: "v.",
    meaning: "連接；聯繫。",
    examples: [
      "The project helped students connect words with actions.",
      "Try to connect the sentence with your own life."
    ]
  },
  actions: {
    ipa: "/ˈækʃnz/",
    pos: "n.",
    meaning: "行動；動作。單數是 action。",
    examples: [
      "Real actions make new words easier to remember.",
      "Your actions are more important than your words."
    ]
  },
  facts: {
    ipa: "/fækts/",
    pos: "n.",
    meaning: "事實。單數是 fact。",
    examples: [
      "Checking facts is part of learning.",
      "The report gave us three important facts."
    ]
  },
  language: {
    ipa: "/ˈlæŋɡwɪdʒ/",
    pos: "n.",
    meaning: "語言。",
    examples: [
      "English is a useful language.",
      "Studying a language takes patience."
    ]
  },
  expected: {
    ipa: "/ɪkˈspektɪd/",
    pos: "v./adj.",
    meaning: "預期；期望；原形是 expect。",
    examples: [
      "The tomatoes needed more sunlight than they expected.",
      "I expected the test to be difficult."
    ]
  },
  watering: {
    ipa: "/ˈwɔːtərɪŋ/",
    pos: "v.-ing/n.",
    meaning: "澆水；灌溉。",
    examples: [
      "Watering too often can hurt the roots.",
      "Watering plants is my job on Monday."
    ]
  }
};

const state = {
  lesson: null,
  enteredWords: [],
  challengeWord: "",
  quizQuestions: [],
  remoteStatus: "idle"
};

const STORAGE_KEY = "dailyEnglishStudioRecords";

const els = {
  weekdayLabel: document.querySelector("#weekdayLabel"),
  sourceLabel: document.querySelector("#sourceLabel"),
  routeNote: document.querySelector("#routeNote"),
  articleTitle: document.querySelector("#articleTitle"),
  sourceLink: document.querySelector("#sourceLink"),
  articleText: document.querySelector("#articleText"),
  wordCount: document.querySelector("#wordCount"),
  printButton: document.querySelector("#printButton"),
  newLessonButton: document.querySelector("#newLessonButton"),
  recordSummary: document.querySelector("#recordSummary"),
  clearRecordsButton: document.querySelector("#clearRecordsButton"),
  wordInput: document.querySelector("#wordInput"),
  buildCardsButton: document.querySelector("#buildCardsButton"),
  clearButton: document.querySelector("#clearButton"),
  studyContent: document.querySelector("#studyContent"),
  vocabCards: document.querySelector("#vocabCards"),
  sentenceStudyList: document.querySelector("#sentenceStudyList"),
  quizList: document.querySelector("#quizList"),
  quizFeedback: document.querySelector("#quizFeedback"),
  copyCheck: document.querySelector("#copyCheck"),
  readCheck: document.querySelector("#readCheck"),
  copyStatus: document.querySelector("#copyStatus"),
  challengeWord: document.querySelector("#challengeWord"),
  rerollWordButton: document.querySelector("#rerollWordButton"),
  sentenceInput: document.querySelector("#sentenceInput"),
  checkSentencesButton: document.querySelector("#checkSentencesButton"),
  sentenceFeedback: document.querySelector("#sentenceFeedback"),
  stepList: document.querySelector("#stepList")
};

function getTodayRoute() {
  const now = new Date();
  const day = now.getDay();
  const weekday = new Intl.DateTimeFormat("zh-CN", {
    year: "numeric",
    month: "long",
    day: "numeric",
    weekday: "long"
  }).format(now);

  if ([1, 3, 5].includes(day)) {
    return { weekday, sourceKey: "bbc", note: "今天按规则进入 BBC 新闻阅读。" };
  }

  if ([2, 4, 6].includes(day)) {
    return { weekday, sourceKey: "zhongkao", note: "今天按规则进入上海中考阅读题练习。" };
  }

  return {
    weekday,
    sourceKey: "mixed",
    note: "今天是周日，原规则未指定来源；已进入混合复习日。"
  };
}

function pickLesson(sourceKey) {
  const pool =
    sourceKey === "mixed" ? lessons : lessons.filter((lesson) => lesson.sourceKey === sourceKey);
  return pool[Math.floor(Math.random() * pool.length)];
}

async function fetchRemoteLesson(forceRefresh = false) {
  const params = new URLSearchParams();
  if (forceRefresh) {
    params.set("force", "1");
    params.set("variant", String(Date.now()));
  }
  const url = `/daily-lesson.json${params.toString() ? `?${params.toString()}` : ""}`;
  const response = await fetch(url, {
    headers: {
      Accept: "application/json"
    }
  });

  if (!response.ok) {
    throw new Error(`Daily lesson API failed: ${response.status}`);
  }

  return response.json();
}

function normalizeLesson(rawLesson) {
  return {
    ...rawLesson,
    sourceKey: rawLesson.sourceKey || rawLesson.route?.sourceKey || "remote",
    sourceLabel: rawLesson.sourceLabel || rawLesson.route?.sourceLabel || "联网阅读材料",
    sourceName: rawLesson.sourceName || rawLesson.provider || "Live source",
    sourceUrl: rawLesson.sourceUrl || "#",
    sentenceTranslations: rawLesson.sentenceTranslations || [],
    grammar: Array.isArray(rawLesson.grammar) ? rawLesson.grammar : []
  };
}

function normalizeWord(word) {
  return word
    .toLowerCase()
    .trim()
    .replace(/^[^a-z]+|[^a-z]+$/g, "");
}

function getWordsFromInput() {
  const words = els.wordInput.value
    .split(/[\s,，;；、]+/)
    .map(normalizeWord)
    .filter(Boolean);
  return [...new Set(words)];
}

function countWords(text) {
  return text.split(/\s+/).filter(Boolean).length;
}

function updateSteps(maxIndex) {
  [...els.stepList.children].forEach((item, index) => {
    item.classList.toggle("done", index < maxIndex);
    item.classList.toggle("active", index === maxIndex);
  });
}

function readRecords() {
  try {
    return JSON.parse(localStorage.getItem(STORAGE_KEY)) || [];
  } catch {
    return [];
  }
}

function writeRecords(records) {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(records));
  } catch {
    // Local storage can be unavailable in strict browser modes; the lesson still works.
  }
}

function renderRecords() {
  const records = readRecords();

  if (!records.length) {
    els.recordSummary.textContent = "还没有保存记录。";
    return;
  }

  const latest = records[0];
  els.recordSummary.innerHTML = `
    已保存 <strong>${records.length}</strong> 次练习。<br>
    最近：${latest.date}<br>
    篇目：${latest.title}<br>
    生词：${latest.words.length ? latest.words.join(", ") : "未填写"}
  `;
}

function saveProgress(stage, extra = {}) {
  if (!state.lesson) return;

  const date = new Intl.DateTimeFormat("zh-CN", {
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit"
  }).format(new Date());
  const key = `${new Date().toISOString().slice(0, 10)}-${state.lesson.id}`;
  const records = readRecords();
  const nextRecord = {
    key,
    date,
    stage,
    title: state.lesson.title,
    source: state.lesson.sourceLabel,
    words: [...state.enteredWords],
    challengeWord: state.challengeWord,
    copied: els.copyCheck.checked,
    readAloud: els.readCheck.checked,
    ...extra
  };
  const existingIndex = records.findIndex((record) => record.key === key);

  if (existingIndex >= 0) {
    records.splice(existingIndex, 1);
  }

  writeRecords([nextRecord, ...records].slice(0, 30));
  renderRecords();
}

async function renderLesson(options = {}) {
  const route = getTodayRoute();
  const forceRefresh = Boolean(options.forceRefresh);
  state.remoteStatus = "loading";
  els.weekdayLabel.textContent = route.weekday;
  els.sourceLabel.textContent = "联网抓取中";
  els.routeNote.textContent = "正在请求后端 /daily-lesson.json，抓取并解析今日材料。";
  els.articleTitle.textContent = "正在生成今日阅读";
  els.sourceLink.href = "#";
  els.sourceLink.textContent = "Live API";
  els.articleText.textContent = "请稍等，后端正在联网抓取来源、解析正文并生成今日学习材料。";
  els.wordCount.textContent = "loading";

  try {
    state.lesson = normalizeLesson(await fetchRemoteLesson(forceRefresh));
    state.remoteStatus = "online";
  } catch (error) {
    console.warn(error);
    state.lesson = pickLesson(route.sourceKey);
    state.remoteStatus = "fallback";
  }

  state.enteredWords = [];
  state.challengeWord = "";

  els.sourceLabel.textContent = state.lesson.sourceLabel;
  els.routeNote.textContent =
    state.remoteStatus === "online"
      ? `${state.lesson.route?.note || route.note} 已通过后端联网抓取与解析，并生成学习改写版。`
      : `${route.note} 后端暂不可用，当前显示本地兜底材料。`;
  els.articleTitle.textContent = state.lesson.title;
  els.sourceLink.href = state.lesson.sourceUrl;
  els.sourceLink.textContent = state.lesson.sourceName;
  els.articleText.textContent = state.lesson.paragraph;
  els.wordCount.textContent = `${state.lesson.wordCount || countWords(state.lesson.paragraph)} words`;
  els.wordInput.value = "";
  els.sentenceInput.value = "";
  els.studyContent.classList.add("hidden");
  els.copyCheck.checked = false;
  els.readCheck.checked = false;
  els.sentenceFeedback.classList.remove("show");
  els.sentenceFeedback.innerHTML = "";
  els.quizFeedback.classList.remove("show");
  els.quizFeedback.innerHTML = "";
  els.quizList.innerHTML = "";
  updateCopyStatus();
  updateSteps(0);
  renderRecords();
}

function getVocabEntry(word) {
  return (
    vocab[word] || {
      ipa: "/请查词典/",
      pos: "word",
      meaning: "這個詞尚未收入本頁詞庫。請先根據上下文猜意思，再查權威詞典確認。",
      examples: [
        `I found the word "${word}" in today's reading passage.`
      ]
    }
  );
}

function renderVocabCards() {
  els.vocabCards.innerHTML = "";

  state.enteredWords.forEach((word) => {
    const entry = getVocabEntry(word);
    const example = getFirstExample(entry, word);
    const card = document.createElement("article");
    card.className = "vocab-card";
    card.innerHTML = [
      "<header>",
      "<div>",
      "<h4>" + escapeHtml(word) + "</h4>",
      "<span class=\"ipa\">" + escapeHtml(entry.ipa) + "</span>",
      "</div>",
      "<span class=\"tag\">" + escapeHtml(entry.pos) + "</span>",
      "</header>",
      "<p>" + escapeHtml(entry.meaning) + "</p>",
      "<ol class=\"examples\"><li>" + escapeHtml(example) + "</li></ol>"
    ].join("");

    els.vocabCards.appendChild(card);
  });
}

function getFirstExample(entry, word) {
  return entry.examples?.[0] || "I found the word \"" + word + "\" in today's reading passage.";
}

function getSentenceItems() {
  const grammarItems = Array.isArray(state.lesson?.grammar) ? state.lesson.grammar : [];
  if (grammarItems.length) {
    return grammarItems.map((item, index) => ({
      sentence: item.sentence || splitIntoSentences(state.lesson.paragraph)[index] || "",
      translation: state.lesson.sentenceTranslations?.[index] || "",
      formula: item.formula || "S + V + O / Adv",
      explanation: item.explanation || "请先找主词 S，再找谓词 V，最后判断宾词 O、补语 C 或状语 Adv。"
    }));
  }

  return splitIntoSentences(state.lesson.paragraph).map((sentence, index) => ({
    sentence,
    translation: state.lesson.sentenceTranslations?.[index] || "",
    formula: "S + V + O / Adv",
    explanation: "请先找主词 S，再找谓词 V，最后判断宾词 O、补语 C 或状语 Adv。"
  }));
}

function splitIntoSentences(text) {
  return (
    String(text || "")
      .match(/[^.!?]+[.!?]+|[^.!?]+$/g)
      ?.map((sentence) => sentence.trim())
      .filter(Boolean) || []
  );
}

function renderSentenceStudy() {
  els.sentenceStudyList.innerHTML = "";
  const items = getSentenceItems();

  items.forEach((item, index) => {
    const card = document.createElement("article");
    card.className = "sentence-study-card" + (index > 0 ? " locked" : "");
    card.dataset.index = String(index);
    card.innerHTML = [
      "<header><span>" + (index + 1) + "</span><strong>" + (index === 0 ? "当前句" : "下一句") + "</strong></header>",
      "<p class=\"sentence-english\">" + escapeHtml(item.sentence) + "</p>",
      "<button class=\"quiet-button sentence-step-button\" type=\"button\" data-action=\"translation\" " + (index > 0 ? "disabled" : "") + ">显示中文翻译</button>",
      "<div class=\"sentence-translation hidden-step\">" + escapeHtml(item.translation || "这一句暂无单独翻译，请对照上下文理解。") + "</div>",
      "<button class=\"quiet-button sentence-step-button\" type=\"button\" data-action=\"grammar\" disabled>显示语法结构</button>",
      "<div class=\"sentence-grammar-detail hidden-step\"><div class=\"syntax-line\">" + renderSyntaxMarkup(item.sentence) + "</div><small>" + escapeHtml(getTenseNote(item)) + "</small></div>"
    ].join("");
    els.sentenceStudyList.appendChild(card);
  });

  els.sentenceStudyList.onclick = handleSentenceStudyClick;
}

function handleSentenceStudyClick(event) {
  const button = event.target.closest("button[data-action]");
  if (!button || button.disabled) return;

  const card = button.closest(".sentence-study-card");
  const action = button.dataset.action;

  if (action === "translation") {
    card.querySelector(".sentence-translation").classList.remove("hidden-step");
    card.querySelector("button[data-action=grammar]").disabled = false;
    button.disabled = true;
    updateSteps(3);
    return;
  }

  if (action === "grammar") {
    card.querySelector(".sentence-grammar-detail").classList.remove("hidden-step");
    card.classList.add("complete");
    button.disabled = true;
    unlockNextSentenceCard(card);
    saveProgress("已学习逐句翻译与语法");
  }
}

function unlockNextSentenceCard(card) {
  const next = card.nextElementSibling;
  if (!next) {
    updateSteps(4);
    return;
  }

  next.classList.remove("locked");
  next.querySelector("button[data-action=translation]").disabled = false;
}

const PREPOSITIONS = new Set(["in", "on", "at", "by", "for", "from", "with", "to", "into", "through", "before", "after", "during", "beside", "near", "of", "about", "than", "over", "under"]);
const DETERMINERS = new Set(["a", "an", "the", "this", "that", "these", "those", "his", "her", "its", "their", "our", "my", "your", "many", "some", "one", "several", "every"]);
const CONJUNCTIONS = new Set(["and", "but", "so", "because", "when", "while", "whether", "or", "where", "who", "that", "if"]);
const MODALS = new Set(["can", "could", "must", "would", "should", "may", "might", "will"]);
const BE_FORMS = new Set(["am", "is", "are", "was", "were", "be", "been", "being"]);
const LINKING_VERBS = new Set(["become", "became", "seem", "seems", "feel", "feels", "look", "looks"]);
const TRANSITIVE_VERBS = new Set(["slow", "reaches", "reach", "wear", "change", "care", "make", "find", "trace", "explain", "save", "trust", "damage", "damaged", "leave", "said", "say", "carry", "carrying", "translate", "translating", "warned", "take", "needed", "need", "shows", "show", "interrupt", "protect", "passes", "pass", "thought", "found", "invited", "join", "discuss", "share", "read", "learned", "guess", "mark", "ask", "opened", "plan", "measured", "chose", "wrote", "expected", "hurt", "keep", "helped", "connect", "taught", "studying", "growing", "trying"]);
const INTRANSITIVE_VERBS = new Set(["spread", "fell", "sleeping", "preparing", "went", "arrive", "arrives", "matters"]);

function renderSyntaxMarkup(sentence) {
  return getAnnotatedWords(sentence)
    .map((item) => "<span class=\"syntax-word\"><u>" + escapeHtml(item.word) + "</u><em>(" + item.code + ")</em></span>")
    .join("<span class=\"syntax-plus\">+</span>");
}

function getAnnotatedWords(sentence) {
  const words = String(sentence || "").match(/[A-Za-z]+(?:-[A-Za-z]+)?|[0-9]+/g) || [];
  const firstVerbIndex = words.findIndex((word, index) => isVerbLike(words, index));

  return words.map((word, index) => ({
    word,
    code: inferGrammarCode(words, index, firstVerbIndex)
  }));
}

function inferGrammarCode(words, index, firstVerbIndex) {
  const lower = words[index].toLowerCase();
  const previous = words[index - 1]?.toLowerCase() || "";
  const next = words[index + 1]?.toLowerCase() || "";

  if (MODALS.has(lower)) return "Modal";
  if (CONJUNCTIONS.has(lower)) return lower === "who" || lower === "where" || lower === "that" ? "Rel" : "Conj";
  if (PREPOSITIONS.has(lower)) return "Prep";
  if (DETERMINERS.has(lower)) return firstVerbIndex >= 0 && index < firstVerbIndex && !hasPrepositionBeforeVerb(words, index, firstVerbIndex) ? "S" : "Det";
  if (BE_FORMS.has(lower)) return next.endsWith("ing") ? "Aux" : "Vl";
  if (LINKING_VERBS.has(lower)) return "Vl";
  if (TRANSITIVE_VERBS.has(lower)) return "Vt.";
  if (INTRANSITIVE_VERBS.has(lower)) return "Vi.";
  if (lower.endsWith("ly")) return "Adv";
  if (previous === "to" && isVerbBase(lower)) return TRANSITIVE_VERBS.has(lower) ? "Vt." : "Vi.";
  if (hasRecentPreposition(words, index, firstVerbIndex)) return "N";
  if (firstVerbIndex === -1 || index < firstVerbIndex) return "S";

  const firstVerb = words[firstVerbIndex]?.toLowerCase() || "";
  if (BE_FORMS.has(firstVerb) || LINKING_VERBS.has(firstVerb)) return "C";
  return "O";
}

function isVerbLike(words, index) {
  const lower = words[index].toLowerCase();
  const next = words[index + 1]?.toLowerCase() || "";
  return MODALS.has(lower) || BE_FORMS.has(lower) || LINKING_VERBS.has(lower) || TRANSITIVE_VERBS.has(lower) || INTRANSITIVE_VERBS.has(lower) || (lower === "had" && next === "to");
}

function isVerbBase(lower) {
  return TRANSITIVE_VERBS.has(lower) || INTRANSITIVE_VERBS.has(lower) || ["use", "write", "study", "learn", "grow"].includes(lower);
}

function hasPrepositionBeforeVerb(words, index, firstVerbIndex) {
  if (firstVerbIndex < 0) return false;
  return words.slice(0, index).some((word) => PREPOSITIONS.has(word.toLowerCase()));
}

function hasRecentPreposition(words, index, firstVerbIndex) {
  const start = Math.max(firstVerbIndex + 1, index - 4, 0);
  return words.slice(start, index).some((word) => PREPOSITIONS.has(word.toLowerCase()));
}

function getTenseNote(item) {
  const sentence = item.sentence.toLowerCase();
  const notes = [];

  if (/\b(was|were)\s+[a-z]+ing\b/.test(sentence)) notes.push("过去进行时：表示过去某一时间正在发生的动作。");
  if (/\b(am|is|are)\s+[a-z]+ing\b/.test(sentence)) notes.push("现在进行时：表示正在进行或当前阶段持续的动作。");
  if (/\b(can|could|must|would|should|may|might|will)\b/.test(sentence)) notes.push("情态动词：后接动词原形，用来表示能力、可能、必须或推测。");
  if (/\b(was|were|had|opened|damaged|helped|learned|taught|found|asked|invited|warned)\b/.test(sentence)) notes.push("一般过去时：用于叙述已经发生的事情。");
  if (/\b(is|are|was|were|be|been)\s+[a-z]+ed\b/.test(sentence)) notes.push("被动语态：be + 过去分词，强调动作的承受者。");
  if (!notes.length) notes.push("一般现在时：用于说明事实、习惯或文章中的普遍判断。");

  return notes.join(" ") + " " + item.explanation;
}

function renderQuiz() {
  state.quizQuestions = buildQuizQuestions();
  els.quizList.innerHTML = state.quizQuestions.map(renderQuizQuestion).join("");
  els.quizFeedback.classList.remove("show");
  els.quizFeedback.innerHTML = "";

  const button = document.createElement("button");
  button.className = "primary-button";
  button.type = "button";
  button.textContent = "提交答案";
  button.addEventListener("click", checkQuizAnswers);
  els.quizList.appendChild(button);
}

function buildQuizQuestions() {
  const items = getSentenceItems();
  const templates = [
    { label: "主词", codes: ["S"] },
    { label: "谓词动词", codes: ["Vt.", "Vi.", "Vl", "Aux", "Modal"] },
    { label: "宾词或补语", codes: ["O", "C"] }
  ];

  return templates.map((template, index) => {
    const item = items[index % items.length];
    const annotated = getAnnotatedWords(item.sentence);
    const answer = annotated.find((part) => template.codes.includes(part.code))?.word || annotated[0]?.word || "以上都不是";
    const options = buildQuizOptions(answer, annotated.map((part) => part.word), index);

    return {
      sentence: item.sentence,
      question: "此句中的" + template.label + "是哪一个？",
      options,
      answer
    };
  });
}

function buildQuizOptions(answer, words, index) {
  const uniqueWords = [...new Set(words.filter((word) => word !== answer))];
  const options = [answer, ...uniqueWords.slice(0, 2), "以上都不是"].slice(0, 4);
  while (options.length < 4) options.splice(options.length - 1, 0, "未出现");
  const shift = index % options.length;
  return [...options.slice(shift), ...options.slice(0, shift)];
}

function renderQuizQuestion(question, index) {
  return [
    "<article class=\"quiz-card\">",
    "<p class=\"quiz-sentence\">" + (index + 1) + ". " + escapeHtml(question.sentence) + "</p>",
    "<fieldset><legend>" + escapeHtml(question.question) + "</legend>",
    question.options
      .map((option, optionIndex) => "<label><input type=\"radio\" name=\"quiz-" + index + "\" value=\"" + escapeHtml(option) + "\" /><span>(" + (optionIndex + 1) + ") " + escapeHtml(option) + "</span></label>")
      .join(""),
    "</fieldset></article>"
  ].join("");
}

function checkQuizAnswers() {
  const results = state.quizQuestions.map((question, index) => {
    const selected = els.quizList.querySelector("input[name=\"quiz-" + index + "\"]:checked")?.value || "";
    return { question, selected, correct: selected === question.answer };
  });
  const score = results.filter((result) => result.correct).length;

  els.quizFeedback.classList.add("show");
  els.quizFeedback.innerHTML = [
    "<h4>语法选择题结果：" + score + " / " + state.quizQuestions.length + "</h4>",
    "<ol>",
    results
      .map((result, index) => "<li>第 " + (index + 1) + " 题：" + (result.correct ? "答对了" : "正确答案是 " + escapeHtml(result.question.answer)) + "。</li>")
      .join(""),
    "</ol>",
    "<p class=\"screenshot-reminder\">请把这一页截屏保留，并发给爸爸。</p>"
  ].join("");
  updateSteps(6);
  saveProgress("已完成语法选择题", { quizScore: score });
}

function escapeHtml(value) {
  return String(value ?? "")
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;");
}

function chooseChallengeWord() {
  if (!state.enteredWords.length) {
    state.challengeWord = "";
    els.challengeWord.textContent = "-";
    return;
  }
  state.challengeWord = state.enteredWords[Math.floor(Math.random() * state.enteredWords.length)];
  els.challengeWord.textContent = state.challengeWord;
}

function buildStudyContent() {
  const words = getWordsFromInput();

  if (!words.length) {
    els.wordInput.focus();
    els.wordInput.placeholder = "请先输入至少一个生词，例如：context, confident";
    return;
  }

  state.enteredWords = words;
  renderVocabCards();
  renderSentenceStudy();
  renderQuiz();
  chooseChallengeWord();
  els.studyContent.classList.remove("hidden");
  updateSteps(2);
  saveProgress("已生成学习内容");
}

function updateCopyStatus() {
  if (els.copyCheck.checked && els.readCheck.checked) {
    els.copyStatus.textContent = "很好，现在可以完成三题语法选择题。";
    updateSteps(5);
    if (state.enteredWords.length) saveProgress("已完成抄写与朗读");
  } else {
    els.copyStatus.textContent = "完成两项后，再进入语法选择题。";
    if (state.enteredWords.length) saveProgress("学习进行中");
  }
}

function splitStudentSentences(text) {
  return text
    .split(/\n+/)
    .map((line) => line.trim())
    .filter(Boolean);
}

function sentenceContainsWord(sentence, word) {
  const escaped = word.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  return new RegExp(`\\b${escaped}\\b`, "i").test(sentence);
}

function detectIssues(sentence, word, index) {
  const issues = [];

  if (!sentenceContainsWord(sentence, word)) {
    issues.push(`第 ${index + 1} 句没有使用抽到的生词 "${word}"。`);
  }

  if (!/^[A-Z]/.test(sentence)) {
    issues.push(`第 ${index + 1} 句句首建议大写。`);
  }

  if (!/[.!?]$/.test(sentence)) {
    issues.push(`第 ${index + 1} 句结尾建议加 . / ? / !。`);
  }

  const checks = [
    {
      pattern: /\bI\s+is\b/i,
      message: `第 ${index + 1} 句中 "I is" 应改为 "I am"。`
    },
    {
      pattern: /\b(you|we|they)\s+is\b/i,
      message: `第 ${index + 1} 句中 you/we/they 后面不要用 is，可按意思改用 are。`
    },
    {
      pattern: /\b(he|she|it)\s+are\b/i,
      message: `第 ${index + 1} 句中 he/she/it 后面不要用 are，可按意思改用 is。`
    },
    {
      pattern: /\b(he|she|it)\s+(go|have|do|make|need|say|read)\b/i,
      message: `第 ${index + 1} 句如果是一般现在时，he/she/it 后的实义动词通常要加 -s 或使用特殊形式。`
    },
    {
      pattern: /\ba\s+[aeiou]/i,
      message: `第 ${index + 1} 句里 a 后接元音音素开头的词时，通常要改成 an。`
    },
    {
      pattern: /\ban\s+[bcdfghjklmnpqrstvwxyz]/i,
      message: `第 ${index + 1} 句里 an 后如果不是元音音素开头，通常要改成 a。`
    },
    {
      pattern: /\b(am|is|are)\s+go\b/i,
      message: `第 ${index + 1} 句里 be + go 应按意思改成 be going 或直接用 go/goes。`
    }
  ];

  checks.forEach((check) => {
    if (check.pattern.test(sentence)) issues.push(check.message);
  });

  return issues;
}

function getModelSentences(word) {
  const entry = getVocabEntry(word);
  const firstExample = getFirstExample(entry, word);
  return [
    firstExample,
    `I can use "${word}" clearly when I write about today's article.`
  ];
}

function checkSentences() {
  if (!state.challengeWord) {
    els.sentenceFeedback.classList.add("show");
    els.sentenceFeedback.innerHTML = "<h4>还没有抽到生词</h4><p>请先输入生词并生成学习内容。</p>";
    return;
  }

  const sentences = splitStudentSentences(els.sentenceInput.value);
  const issues = [];

  if (sentences.length < 2) {
    issues.push("请至少输入两句，每句单独一行。");
  }

  sentences.slice(0, 2).forEach((sentence, index) => {
    issues.push(...detectIssues(sentence, state.challengeWord, index));
  });

  const models = getModelSentences(state.challengeWord);

  els.sentenceFeedback.classList.add("show");
  els.sentenceFeedback.innerHTML = `
    <h4>${issues.length ? "需要订正的地方" : "基础检查通过"}</h4>
    ${
      issues.length
        ? `<ul>${issues.map((issue) => `<li>${issue}</li>`).join("")}</ul>`
        : "<p>两句都包含目标生词，并通过了基础大小写、标点和常见语法检查。</p>"
    }
    <h4>可参考的正确句子</h4>
    <ol>${models.map((sentence) => `<li>${sentence}</li>`).join("")}</ol>
  `;
  updateSteps(7);
  saveProgress("已检查造句", { sentenceCount: sentences.length, issueCount: issues.length });
}

els.printButton.addEventListener("click", () => window.print());
els.newLessonButton.addEventListener("click", () => renderLesson({ forceRefresh: true }));
els.clearRecordsButton.addEventListener("click", () => {
  writeRecords([]);
  renderRecords();
});
els.buildCardsButton.addEventListener("click", buildStudyContent);
els.clearButton.addEventListener("click", () => {
  els.wordInput.value = "";
  els.wordInput.focus();
});
els.copyCheck.addEventListener("change", updateCopyStatus);
els.readCheck.addEventListener("change", updateCopyStatus);
els.rerollWordButton.addEventListener("click", () => {
  chooseChallengeWord();
  els.sentenceInput.value = "";
  els.sentenceFeedback.classList.remove("show");
  if (state.enteredWords.length) saveProgress("已更换造句生词");
});
els.checkSentencesButton.addEventListener("click", checkSentences);

renderLesson();
renderRecords();
